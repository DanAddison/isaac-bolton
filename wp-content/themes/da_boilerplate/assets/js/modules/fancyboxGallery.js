jQuery(function($) {
    // initiate Fancybox
    $('[data-fancybox="gallery"]').fancybox({
        // Options
        slideShow : false,
        thumbs : false,
        loop : true,
        infobar : true,
    });
});